CREATE TABLE Rule (
    date      TEXT NOT NULL,
    symbol    TEXT NOT NULL,
    pattern   TEXT NOT NULL,
    type      TEXT NOT NULL,
    logic     TEXT NOT NULL,
    date_time TEXT NOT NULL,
    period    TEXT NOT NULL
);
